// Proxy types for the vehicle detail view (KISS style)
import type { Encar_publicTypes } from '@carapis/api';

export type VehicleDetail = Encar_publicTypes.VehicleWebDetail;

// Add more as needed for detail view features
